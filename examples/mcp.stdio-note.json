{
  "_comment": "Streamable-HTTP MCP is live at POST /mcp. Prefer the url form in examples/mcp.json. This stdio wrapper is only useful if a client cannot speak HTTP MCP:",
  "mcpServers": {
    "shipcheck": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "http://127.0.0.1:8787/mcp"],
      "env": {
        "MCP_AUTHORIZATION": "Bearer YOUR_API_KEY"
      }
    }
  }
}
